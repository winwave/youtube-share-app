import { useState } from "react";
import {Layout} from "../../components/Layout/Layout";

export const HomePage = () => {

    return (
        <Layout>
            <div>
                HomePage
            </div>
        </Layout>
    )
}